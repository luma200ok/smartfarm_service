# env / secrets
.env
.env.*
!.env.example
application-local.yml

# Java / Gradle
backend/build/
backend/.gradle/
backend/out/
*.class

# Node / Next
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/next-env.d.ts

# IDE / OS
.idea/
*.iml
.vscode/
.DS_Store

# local-only docs (handoff 등)
docs/_local/

# 레포 루트의 스크린샷·캡처 산출물(검증용으로 만들었다 지우는 파일).
# 시안 적용 사이클(#133~#147)에서 브라우저 캡처를 루트에 반복 생성했고,
# 매번 수동으로 지워야 커밋에 섞이지 않았다. 하위 디렉터리의 정식 애셋은 영향 없다.
/*.png
/*.jpg
/*.jpeg
